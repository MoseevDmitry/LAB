import 'package:flutter/material.dart';

class ColorCont extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      width: 150,
      height: 10,
      color: Colors.green,

    );
  }
}
